import Header from "./Header";
import Footer from "./Footer";

export { Header, Footer };
//index.js is the entry file the app looks for
//when it looks at this folder it will get header and footer export
